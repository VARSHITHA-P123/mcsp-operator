---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: mcpscustomers.mcsp.mcsp.io
spec:
  group: mcsp.mcsp.io
  names:
    kind: MCPSCustomer
    listKind: MCPSCustomerList
    plural: mcpscustomers
    singular: mcpscustomer
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MCPSCustomer is the Schema for the mcpscustomers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MCPSCustomerSpec defines the desired state of MCPSCustomer.
            properties:
              customerName:
                description: CustomerName is the name of the customer
                type: string
            required:
            - customerName
            type: object
          status:
            description: MCPSCustomerStatus defines the observed state of MCPSCustomer.
            properties:
              deployed:
                description: Deployed indicates if the customer is deployed
                type: boolean
              message:
                description: Message gives the current status message
                type: string
              url:
                description: URL is the live URL of the customer application
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
